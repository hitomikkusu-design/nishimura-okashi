# Google Maps JavaScript API key used by client/src/components/Map.tsx.
# If unset, the map on the 店舗案内 (Access) page falls back to a
# "Googleマップで開く" link instead of an embedded map.
VITE_GOOGLE_MAPS_API_KEY=
